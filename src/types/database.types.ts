/**
 * Tipos generados por Supabase a partir del esquema real.
 *
 * Este archivo es un PLACEHOLDER mientras no hay un proyecto de Supabase
 * conectado. Una vez creado el proyecto y aplicadas las migraciones de
 * supabase/migrations, regeneralo con:
 *
 *   npx supabase gen types typescript --project-id <tu-project-id> > src/types/database.types.ts
 *
 * No lo edites a mano despues de eso.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any
